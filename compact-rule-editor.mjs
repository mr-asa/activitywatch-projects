import {projectRules,ruleMatches,clipRule} from './rule-engine.mjs';
import {groupRules,expandGroup} from './rule-groups.mjs';
import {matchTitle,matchUrl} from './projects-core.mjs';
export function setupRuleEditor({state}){
 const $=id=>document.getElementById(id);let current=document.querySelector('label[for="project-keywords"]');while(current&&current.id!=='form-error'){const next=current.nextElementSibling;current.remove();current=next;}
 const meta=document.createElement('div');meta.className='project-meta';const name=document.createElement('div'),color=document.createElement('div');const nameLabel=document.querySelector('label[for="project-name"]');nameLabel.before(meta);name.append(nameLabel,$('project-name'));color.append(document.querySelector('label[for="project-color"]'),document.querySelector('.color-row'));meta.append(name,color);
 const section=document.createElement('section');section.id='rule-editor';section.innerHTML='<div class="section-heading"><div><h3>Automatic rules</h3><p class="field-help">One alternative per line. Patterns in a group share the same application and date limits.</p></div><button type="button" id="add-rule">+ Add rule group</button></div><div id="rule-rows"></div><p class="field-help">Blank application = any app. Blank dates = no limit. Date limits include both selected dates.</p>';$('form-error').before(section);
 const apps=document.createElement('datalist');apps.id='recorded-apps';document.body.append(apps);
 section.addEventListener('input',()=>{$('form-error').textContent='';});
 function add(input={}){const group={type:'title',mode:'text',ignoreCase:true,from:'',through:'',appFilter:'',entries:[],...input};const card=document.createElement('div');card.className='rule-row compact-rule';card._entries=group.entries;
 card.innerHTML='<div class="compact-controls"><label>Field<select data-key="type" aria-label="Rule field"><option value="title">Window title</option><option value="url">Browser URL</option></select></label><label>Match mode<select data-key="mode" aria-label="Match mode"><option value="text">Plain text</option><option value="regex">Regex</option></select></label><label class="app-filter-label">Application (optional)<input data-key="appFilter" aria-label="Application filter" list="recorded-apps" placeholder="Any app · e.g. Telegram or maya.exe"></label><label class="check-label"><input data-key="ignoreCase" type="checkbox"> Ignore case</label></div><div class="compact-body"><label>Patterns · one per line<textarea data-key="patterns" rows="3" aria-label="Rule pattern" placeholder="LOOKDEV&#10;ANIMATION"></textarea></label><div class="compact-dates"><label>Valid from<input data-key="from" aria-label="Valid from" type="date"></label><label>Valid through<input data-key="through" aria-label="Valid through" type="date"></label></div></div><div class="compact-footer"><p class="rule-mode-hint field-help"></p><div class="rule-actions"><button type="button" class="preview-rule">Preview matches</button><button type="button" class="remove-rule">Remove group</button></div></div><div class="rule-preview" role="status"></div>';
 for(const key of ['type','mode','appFilter','from','through'])card.querySelector(`[data-key="${key}"]`).value=group[key];card.querySelector('[data-key="ignoreCase"]').checked=group.ignoreCase;const area=card.querySelector('[data-key="patterns"]');area.value=group.entries.map(r=>r.pattern).join('\n');area.rows=Math.min(6,Math.max(3,group.entries.length));
 const hint=()=>{const mode=card.querySelector('[data-key="mode"]').value,type=card.querySelector('[data-key="type"]').value;card.querySelector('.app-filter-label').hidden=type!=='title';card.querySelector('.rule-mode-hint').textContent=mode==='regex'?'One JavaScript regex per line, without / delimiters.':type==='url'?'Full links; subpages and matching query parameters included.':'Title fragments. Case-insensitive text also ignores [modified].';};hint();for(const key of ['type','mode'])card.querySelector(`[data-key="${key}"]`).onchange=hint;
 card.querySelector('.remove-rule').onclick=()=>card.remove();card.querySelector('.preview-rule').onclick=()=>{const out=card.querySelector('.rule-preview');out.replaceChildren();try{const rules=readRow(card),type=rules[0].type;const events=type==='title'?state.data?.windows||[]:(state.data?.browsers||[]).flatMap(b=>b.events);const matches=[...new Set(events.filter(e=>{const start=Date.parse(e.timestamp);return rules.some(rule=>clipRule([[Math.max(start,state.start),Math.min(start+e.duration*1000,state.end)]],rule).length&&ruleMatches(rule,e.data[type==='title'?'title':'url'],matchTitle,matchUrl,e.data.app));}).map(e=>type==='title'?`${e.data.title} · ${e.data.app}`:e.data.url))];out.textContent=`${matches.length} matching ${type==='title'?'titles':'URLs'} in the loaded report period.`;const ul=document.createElement('ul');for(const value of matches.slice(0,8)){const li=document.createElement('li');li.textContent=value;ul.append(li);}out.append(ul);}catch(e){out.textContent=e.message;}};$('rule-rows').append(card);return card;
 }
 function readRow(card){const input={};for(const key of ['type','mode','appFilter','patterns','from','through'])input[key]=card.querySelector(`[data-key="${key}"]`).value;input.ignoreCase=card.querySelector('[data-key="ignoreCase"]').checked;return expandGroup(input,card._entries);}
 $('add-rule').onclick=()=>add();return {load(p,seed){$('rule-rows').replaceChildren();for(const g of groupRules([...(p?projectRules(p):[]),...(seed?[seed]:[])]))add(g);apps.replaceChildren(...[...new Set((state.data?.windows||[]).map(e=>e.data.app).filter(Boolean))].sort().map(app=>new Option(app,app)));},read(){return [...$('rule-rows').children].flatMap(readRow);},add};
}
